<UserControl x:Class="WpfTestApp.Pages.Texts.TextsPageView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="300"
        d:DesignWidth="300"
             x:Name="ButtonsPage"
             AutomationProperties.AutomationId="TextsPage">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <GroupBox Grid.Row="0"
                Header="Some Text Blocks">
            <GroupBox x:Name="SomeTextBoxes">
                <StackPanel x:Name="Panel">
                    <TextBlock>General</TextBlock>
                    <TextBlock>Layout</TextBlock>
                    <TextBlock>Runs</TextBlock>
                    <TextBlock>Analysis</TextBlock>
                </StackPanel>
            </GroupBox>
        </GroupBox>
    </Grid>
</UserControl>